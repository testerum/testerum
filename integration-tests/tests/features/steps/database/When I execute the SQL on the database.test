test-def: When I execute the SQL on the database

    step: When I execute the SQL <<{{pet_clinic}}>> on the database <<file:db/petclinic-connection.rdbms.connection.yaml>>
        var pet_clinic = <<
            DELETE FROM roles WHERE username = 'admin';
            DELETE FROM users Where username = 'admin';
            
            INSERT INTO users (username, password, enabled) VALUES ('admin', 'admin', 1) ;
            
            INSERT INTO roles
                (username, role)
            VALUES
                ('admin', 'ROLE_ADMIN'),
                ('admin', 'ROLE_OWNER_ADMIN'),
                ('admin', 'ROLE_VET_ADMIN') ;
        >>
