import {Serializable} from "../../infrastructure/serializable.model";

export interface UserProfile extends Serializable<any> {
    // todo Ionut: delete this (and implementations, and marshaller, etc.)
}
