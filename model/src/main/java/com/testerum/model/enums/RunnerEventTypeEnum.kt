package com.testerum.model.enums

enum class RunnerEventTypeEnum {
    SUITE_STARTED,
    SUITE_ENDED,
    TEST_STARTED,
    TEST_ENDED,
    STEP_STARTED,
    STEP_LOG,
    STEP_ENDED,
    SCENARIO_RESULTS,

    ERROR
}